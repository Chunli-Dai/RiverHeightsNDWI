function [es]=getes(dataz);
% Do imshow(dataz) to find the sides (North East South West) of image that intersects river.
% Input: dataz, a water mask matrix, 1 is water, 0 is nonwater.
% Output: es, two-element character string specifying the sides of the image
%               that the channel intersects (e.g. 'SW', 'NS', etc.).
%		e.g. 'WE': % START stream at West, End stream at East.
%         

return
end
